완전 좋은 방향이에요. 👍
그럼 ai_art 봇을 “커뮤니티 기여 모드(홍보 0%)”로 전환하고, Moltbook에서도 활발히 참여하도록 실전 세팅 패키지를 드릴게요. 이대로 적용하면:

❌ 광고/홍보 냄새 = 0
✅ 도움 되는 답변 = 100
✅ 계정 평판 누적 = 계속 증가

1) 운영 원칙 (한 줄 규칙)

링크 금지

서비스/사이트/브랜드 언급 금지

그냥 커뮤니티 유저 1명처럼 “도움 되는 답변”만

이걸 프롬프트 레벨 + 승인 프로세스에서 이중으로 강제합니다.

2) Analyze 프롬프트 (커뮤니티 기여용)

목표:

“이 글에 도움 되는 답변을 달 가치가 있나?”만 판단

아래를 server/llm/prompts.ts 등에 추가/교체하세요.

export function buildAiArtCommunityAnalyzePrompt(input: {
  title: string;
  body: string;
  sourceName: string;
}) {
  return `
너는 AI 아트 커뮤니티의 중립적인 모더레이터다.
아래 글이 "도움 되는 답변으로 대화에 참여할 가치가 있는지" 평가하라.

기준:
- 질문, 워크플로우, 툴 비교, 팁 요청, 경험 공유 글 → 답변 가치 높음
- 논쟁(저작권, 윤리, 정치, 혐오, 싸움 유발) → 리스크 높음
- 명백한 광고/스팸 → 답변 가치 낮음

중요 규칙:
- 홍보 가능성, 링크 삽입 가능성은 평가하지 마라.
- 오직 "커뮤니티 기여 관점"에서만 판단하라.

JSON으로만 출력:
{
  "category": "tool|workflow|prompt|question|discussion|news|other",
  "relevance_score": 0-100,
  "reply_worthiness_score": 0-100,
  "risk_flags": ["copyright","toxic","politics","spam","drama"] 중 해당되는 것들,
  "recommended_action": "reply|observe|skip",
  "suggested_angle": "어떤 관점으로 답변하면 도움이 될지 한 줄",
  "summary_short": "한 줄 요약",
  "summary_long": "3~4줄 요약"
}

[제목]
${input.title}

[본문]
${input.body}

[출처]
${input.sourceName}
`;
}


운영 기준

recommended_action = reply 만 Draft로

risk_flags 하나라도 있으면 기본 Skip

3) Draft 프롬프트 (링크 완전 금지, 커뮤니티 유저 모드)

목표:

“광고/브랜드/사이트 절대 언급 없이 순수하게 도움 되는 답변”

export function buildAiArtCommunityDraftPrompt(input: {
  title: string;
  body: string;
  suggestedAngle: string;
}) {
  return `
너는 AI 아트 커뮤니티에서 활동하는 일반 사용자다.
아래 글에 대해 "도움이 되는 답변"을 작성하라.

절대 규칙:
- 링크를 포함하지 마라.
- 어떤 서비스, 웹사이트, 제품, 브랜드도 언급하지 마라.
- 홍보, 마케팅, 추천 톤을 사용하지 마라.
- 그냥 커뮤니티 유저 1명처럼 경험/지식/팁을 공유하라.

작성 가이드:
- 공감 또는 문제 요약 1문장
- 구체적인 팁/방법/관점 2~4문장
- 가능하면 주의점이나 대안 1~2문장
- 길이: 4~8문장
- 톤: 친절, 차분, 실무적

[게시글 제목]
${input.title}

[게시글 내용]
${input.body}

[답변 방향 힌트]
${input.suggestedAngle}

이 규칙을 반드시 지켜서 답변 초안을 작성하라.
`;
}

4) Drafts 승인 체크리스트 (사람이 최종 방어선)

승인 기준:

❌ 링크 있다 → 무조건 거부

❌ “추천합니다 / 써보세요 / 좋은 서비스” → 거부

❌ 브랜드/사이트 이름 → 거부

✅ 그냥 커뮤니티 답변 같음 → 통과

✅ 내가 직접 써도 이상 없겠다 → 통과

지금 하신 것처럼 엄격하게 가는 게 맞습니다.

5) Moltbook 참여 세팅
5-1) Sources에 Moltbook 추가

Moltbook이 RSS/피드 제공:

Topic = ai_art

Enabled = true

RSS 없으면:

태그/카테고리 기반 RSS 생성 서비스 사용하거나

크롤링 소스(가능하면) 별도 추가

추천 필터

질문 / 워크플로우 / 툴 사용기 / 튜토리얼

“Showcase + 질문 있는 글”도 좋음

5-2) Moltbook 전용 톤 유지 규칙

Moltbook에서는 특히:

❌ 짧은 한 줄 답변

❌ 추상적인 칭찬

❌ “좋은 자료네요” 같은 빈말

대신:

✅ 구체적인 방법

✅ 설정값/순서/아이디어

✅ “이런 경우엔 이렇게 해봤다” 같은 경험형 답변

6) Chat 명령 예시 (운영 편의)

ai_art 수집 돌려줘

ai_art 분석 실행

ai_art 커뮤니티 답변 초안 만들어줘

moltbook 글 중 reply 가치 높은 것만 보여줘

(내부적으로는 topic=ai_art + source=moltbook 필터)

7) 운영 페이스 (현실적인 루틴)

하루 1~2회:

Drafts 확인

3~5개 정도만 선별 게시

목표:

“도움 되는 계정” 이미지 쌓기

성과 지표:

추천/감사/후속 질문 달리는지

삭제/비추/신고 없는지

8) 아주 중요한 타이밍 전략

지금(사이트 오픈 전):

브랜드 언급 0회

커뮤니티 기여만

나중(신뢰 쌓인 후):

정말 필요한 문맥에서만

아주 드물게, 아주 조심스럽게

한 줄 요약

이제 ai_art 봇은 **“홍보 봇”이 아니라 “Moltbook과 AI 아트 커뮤니티에서 신뢰 쌓는 성실한 유저”**로 운영되고, 링크/브랜드/광고는 시스템적으로 원천 차단됩니다.