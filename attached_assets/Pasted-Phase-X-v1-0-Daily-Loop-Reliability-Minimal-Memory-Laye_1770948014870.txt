Phase X v1.0 — Daily Loop Reliability + Minimal Memory Layer

목표:

“약속한 시간에 반드시 읽을 수 있는 리포트” 보장

리포트에 변화 추적(메모리 레이어) 추가

어떤 경우에도 무한 running 없음

0️⃣ 절대 준수 원칙

리포트는 3초 이내 Fast Report 표시

LLM 실패해도 Fast Report 유지

0 items → 반드시 Status Report 생성

Full Report 업그레이드는 백그라운드

어떤 경우에도 30초 이상 running 금지

1️⃣ Daily Loop 신뢰 지표 구현
1-1. job_runs 확장

job_runs 테이블에 다음 필드 추가:

durationMs (integer)

errorReason (text | null)

stage ("fast" | "full" | "status")

itemsCount (integer)

1-2. 리포트 생성 시 기록

generateReportForProfile() 수정:

Fast Report 생성 직후:

job_runs insert:

status = "success"

stage = "fast"

durationMs = 실제 시간

itemsCount 기록

Full 업그레이드 성공 시:

기존 report update

job_runs insert:

stage = "full"

실패/타임아웃 시:

job_runs insert:

status = "failed"

errorReason 저장

1-3. Dashboard 신뢰 카드 추가

API:

GET /api/diagnostics/daily-loop

반환:

{
lastRunAt,
successRate7d,
avgGenerationTimeMs,
lastFailureReason
}

계산 로직:

최근 7일 job_runs 중 stage="fast" 기준

successRate = success / total

avgGenerationTime = 평균 durationMs

UI:

Dashboard 상단에 카드 추가:

📊 Daily Reliability

Last Run: 09:00

7d Success: 96%

Avg Time: 1.8s

Last Issue: none

2️⃣ Minimal Memory Layer v0.1
2-1. 새 테이블

report_metrics

id

reportId

profileId

itemCount

keywordSummary (JSON)

sourceDistribution (JSON)

createdAt

2-2. Fast Report 생성 시 통계 계산

itemCount

소스별 카운트

제목 기반 단순 키워드 추출 (빈도 상위 5개)

keywordSummary 예시:

{
"AI": 12,
"Nvidia": 5,
"Policy": 3
}

sourceDistribution 예시:

{
"Reuters": 10,
"Reddit": 7
}

report_metrics insert

2-3. 7일 변화 계산 API

GET /api/reports/:profileId/trends

로직:

최근 7개 report_metrics 조회

키워드 변화율 계산

반복 등장 키워드 상위 5개

응답 예:

{
trendingUp: ["AI", "Semiconductor"],
recurring: ["Nvidia", "OpenAI"],
sourceShift: { redditIncrease: true }
}

2-4. Full Report에 변화 블록 추가

Full Report 하단에 자동 삽입:

📊 7일 변화 요약

AI 언급 +23%

Nvidia 4일 연속 등장

Reddit 비중 증가

3️⃣ Fast Report 구조 강제

Fast Report 템플릿 표준화:

제목

오늘의 수집 요약 (3줄)

주요 항목 5~10개

키워드 요약

빈 데이터 금지.

4️⃣ 무한 running 완전 제거 보강

Pipeline 실행 로직 수정:

서버 25초 초과 → 자동 종료 메시지 반환

클라이언트 12초 무응답 → stall toast 표시

30초 절대 종료

실패 시:

"Fast briefing delivered. Full analysis will continue in background."

5️⃣ 완료 기준 (Definition of Done)

Fast Report 항상 3초 내 생성

0 items 시 Status Report 생성

Dashboard에 Daily Reliability 카드 표시

리포트 하단에 7일 변화 요약 표시

무한 running 재현 불가

Postgres + SQLite 모두 동작

6️⃣ 작업 순서

job_runs 확장

report_metrics 테이블 생성

Fast Report 통계 계산 추가

Trends API 구현

Dashboard 카드 UI

Full Report에 변화 블록 삽입

Pipeline timeout 보강

이 작업의 목적

메이커를:

RSS 리더 → 관찰 시스템으로

단순 자동화 → 신뢰 가능한 일상 도구로

진화시키는 단계입니다.